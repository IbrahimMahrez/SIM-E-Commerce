# SIM Store — React

React application powered by Vite.

## Run locally

```powershell
cmd /c npm run dev
```

Open the local URL shown in the terminal (normally `http://localhost:5173`).

The API server must run on `http://localhost:3000` for products, authentication, and cart features.

## Production build

```powershell
cmd /c npm run build
```
